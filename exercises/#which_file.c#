#include "simple_shell.h"

int main(int argc, char *argv[])
{
	unsigned int i;
	struct stat st;
	char *path, *tok;

	if (ac < 2)
	   exit (0);
	i = 1;
	path = getenv("PATH");
	tok = strtok(path, ":");
	while ()
	{
		
	}
}